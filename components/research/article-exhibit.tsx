import { TableScroll } from "@/components/ui/table-scroll";
import type { ResearchExhibit } from "@/lib/types";

export function ArticleExhibit({ exhibit }: { exhibit: ResearchExhibit }) {
  return <figure className="border-y border-line bg-mist/50 py-8"><figcaption><p className="eyebrow">Research exhibit</p><h2 className="mt-3 text-2xl font-semibold text-navy">{exhibit.title}</h2><p className="mt-3 max-w-2xl text-sm leading-6 text-slate">{exhibit.description}</p></figcaption><TableScroll className="mt-6 border border-line bg-white" label={`${exhibit.title}, scroll horizontally if needed`}><table className="w-full min-w-[650px] border-collapse text-left"><thead><tr className="bg-navy text-[10px] uppercase tracking-[0.1em] text-white">{exhibit.columns.map((column) => <th className="px-4 py-3 font-semibold" key={column} scope="col">{column}</th>)}</tr></thead><tbody>{exhibit.rows.map((row, rowIndex) => <tr className="border-t border-line" key={`${row[0]}-${rowIndex}`}>{row.map((cell, cellIndex) => cellIndex === 0 ? <th className="px-4 py-4 text-sm font-semibold text-navy" key={`${cell}-${cellIndex}`} scope="row">{cell}</th> : <td className="px-4 py-4 text-sm text-slate" key={`${cell}-${cellIndex}`}>{cell}</td>)}</tr>)}</tbody></table></TableScroll><p className="mt-4 text-xs leading-5 text-slate"><span className="font-semibold text-navy">Note:</span> {exhibit.note}</p></figure>;
}
